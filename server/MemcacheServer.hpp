#pragma once

#include "Item.hpp"

#include <unordered_map>
#include <unordered_set>

#include "noncopyable.hpp"
#include "mutexLock.hpp"

class MemcacheServer
{
private:
    /* data */
public:
    MemcacheServer(/* args */);
    ~MemcacheServer();
};

MemcacheServer::MemcacheServer(/* args */)
{
}

MemcacheServer::~MemcacheServer()
{
}
